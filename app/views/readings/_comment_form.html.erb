<%= form_for @comment, url: reading_comment_path(reading) do |f| %>
  <%= f.text_area :comment, rows: "4", placeholder: "Add comment", autofocus: true %>
  <%= f.submit "Submit", class: "button tiny" %>
<% end %>